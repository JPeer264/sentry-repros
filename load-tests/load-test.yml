config:
  target: "http://localhost:3333"
  phases:
    - duration: 900
      arrivalRate: 30
      name: "Sustained load"

scenarios:
  - name: "Normal endpoint with nested spans"
    weight: 10
    flow:
      - get:
          url: "/"
