{
  "name": "temp-cloudflare-20409",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc --config wrangler-second.jsonc",
    "deploy": "wrangler deploy",
    "deploy:upstream": "wrangler deploy --config wrangler-second.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260422.1",
    "wrangler": "^4"
  },
  "dependencies": {
    "@sentry/cloudflare": "10.49.0"
  }
}
