{
  "name": "test-issue-12938",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reproduction for getsentry/sentry-javascript#12938 - Screenshot fallback when display-capture is restricted",
  "scripts": {
    "dev": "node server.mjs",
    "dev:parcel": "parcel serve src/host.html --port 8766",
    "build": "parcel build src/host.html --dist-dir dist"
  },
  "devDependencies": {
    "http-proxy": "^1.18.1",
    "parcel": "^2.16.4"
  },
  "dependencies": {
    "@sentry/browser": "^10.48.0"
  }
}
